namespace Eloi
{
    /// <summary>
    /// I am an interface that contain a settable path but without knowing what is containted in it.
    /// </summary>
    public interface I_PathTypeCompletePathSet
    {
        void SetPath(string path);
    }
}